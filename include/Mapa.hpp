#pragma once
#include <SDL2/SDL.h>
#include <stdio.h>

class Mapa
{
private:
    /* data */
public:
    Mapa(/* args */);
    ~Mapa();
};

Mapa::Mapa(/* args */)
{
}

Mapa::~Mapa()
{
}
