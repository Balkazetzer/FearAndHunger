#include "../include/Rectangulo.h"
#include "../include/Rectangulo.h"

Rectangulo::Rectangulo() 
{
    // Initialize your variables here
    _x = 0;
    _y = 0;
    _ancho = 0;
    _alto = 0;
}

Rectangulo::Rectangulo(int x, int y, int ancho, int alto) : _x(x), _y(y), _ancho(ancho), _alto(alto)
{
    // Definition with code
}
